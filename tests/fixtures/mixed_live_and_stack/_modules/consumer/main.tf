# Placeholder for the consumer Terraform module.
# This file exists so the `_modules/consumer/` directory is tracked by git
# and so live units can reference it via `terraform { source = "..." }`.
# It intentionally does NOT contain a `terragrunt.hcl`; otherwise discovery
# would pick it up as a planable unit.
